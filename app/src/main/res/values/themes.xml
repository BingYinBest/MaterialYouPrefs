<?xml version="1.0" encoding="utf-8"?>
<!--
  M5 cleanup: `Theme.MaterialYouPrefs` was flagged by lint UnusedResources.
  The app uses full-screen Compose (`setContent { ... }` in
  MainActivity) with its own MaterialTheme, so no Activity-level
  theme is referenced. Kept as an empty <resources> root.
-->
<resources />
