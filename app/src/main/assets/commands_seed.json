# PLACEHOLDER — this file is intentionally NOT valid JSON.
#
# The real payload (9795 bytes / 17 avbtool subcommands / aospHead=386fb904)
# could not be pushed via the MCP gateway: the gateway re-parses any content
# starting with '{' as a JSON object, so a valid JSON seed string always
# fails with `Expected string, received object`.
#
# Real blob: local git commit 79903e5 on branch feat/avbtool in
# /root/MaterialYouPrefs (authoritative source).
#
# Restore strategy — pick one:
#   1. User runs locally:
#        cd /root/MaterialYouPrefs
#        git checkout feat/avbtool
#        git push --force-with-lease origin feat/avbtool
#   2. User opens https://github.com/BingYinBest/MaterialYouPrefs
#      and pastes content from dev-log/TODO.md task T-M2.5-seed.
#
# AvbToolRunnerImpl / CommandRepository MUST handle this case:
#   - if assets/commands_seed.json starts with '#' or fails JSON.parse,
#     log a warning and fall back to builtin CommandSeedModels.builtinCommands()
#   (see ADR-013 + Task T-M2.5-seed in dev-log/TODO.md)
